"""
AI News - 包初始化

Sub-packages:
  interfaces/ — shared types (i18n)
  engine/     — AI engine + data processing
  frontend/   — HTML page generators
  api/        — FastAPI application
  plugins/    — provider registry
  cli/        — CLI wrappers
"""
